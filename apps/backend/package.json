{
  "name": "@workspace/backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "convex dev",
    "logs": "convex logs",
    "lint": "tsc -p convex && oxlint --fix",
    "auth:generate": "pnpm dlx auth generate --config convex/betterAuth/auth.ts --output convex/betterAuth/schema.gen.ts"
  },
  "dependencies": {
    "@convex-dev/better-auth": "catalog:",
    "better-auth": "catalog:",
    "convex": "^1.39.1",
    "convex-helpers": "^0.1.118",
    "evlog": ">=2.10.0 <2.11.0"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "globals": "^17.6.0",
    "typescript": "catalog:"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "@tailwindcss/oxide"
    ]
  }
}
