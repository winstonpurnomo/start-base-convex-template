# AGENTS.md

## Code Quality

For linting and formatting, check `ULTRACITE.md` for code quality standards.

## Icons

- Always use the `Icon`-suffixed variant when importing from `lucide-react` (e.g. `ArrowRightIcon`, not `ArrowRight`)
